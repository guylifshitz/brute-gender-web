<%= form_for category_word, remote: true do |f| %>
  <%= f.check_box :include_word %>
  <%= f.label category_word.word[:word] %>
  <%= link_to "(remove)", category_word, method: :delete, data: {confirm: "Are you sure?"}, remote: true %>
<% end %>
